% =============================================================================
% SLIDE 2: HAZARD DETECTION
% =============================================================================
\begin{frame}[fragile]{Hazard Detection}
    \begin{itemize}
        \item \textbf{Nhiệm vụ:} Phát hiện xung đột dữ liệu không thể cứu bằng Forwarding (Load-Use) và quản lý trạng thái đóng băng toàn bộ CPU khi Bus bộ nhớ bận (\texttt{mem\_wait}).
    \end{itemize}
    
    \begin{block}{Mã nguồn Verilog - Phát hiện và Quản lý chốt chặn}
\begin{lstlisting}
// --- PASTE CODE LOGIC LOAD-USE STALL VA` QUAN LY' HOLD/CLEAR TAI DAY ---

\end{lstlisting}
    \end{block}
    
    \begin{itemize}
        \item \textbf{Giải thuật:} Khi xảy ra xung đột, tầng \texttt{IF/ID} bị giữ nguyên trạng thái (\texttt{hold}), đồng thời chèn một bong bóng \texttt{NOP} bằng cách xóa tầng \texttt{ID/EX} (\texttt{clear}).
    \end{itemize}
\end{frame}

% =============================================================================
% SLIDE 3: FORWARDING LOGIC
% =============================================================================
\begin{frame}[fragile]{Forwarding}
    \begin{itemize}
        \item \textbf{Nhiệm vụ:} Tránh Stall bừa bãi bằng cách bơm trực tiếp kết quả từ tầng \texttt{EX/MEM} hoặc \texttt{MEM/WB} về đầu vào ALU nếu phát hiện lệnh sau đọc thanh ghi mà lệnh trước chuẩn bị ghi.
    \end{itemize}

    \begin{block}{Mã nguồn Verilog - Khối tổ hợp Forwarding chọn toán hạng ALU}
\begin{lstlisting}
// --- PASTE CODE LOGIC KIEM TRA DIEU KIEN FORWARD (CO' CHECK x0) TAI DAY ---

\end{lstlisting}
    \end{block}

    \begin{alertblock}{Đặc điểm cốt lõi}
        Bắt buộc phải kiểm tra điều kiện loại trừ thanh ghi đặc biệt \textbf{\texttt{x0}} (\texttt{rd\_addr != 5'd0}). Nếu lệnh trước ghi vào \texttt{x0}, mạch bỏ qua không forward để bảo toàn giá trị $0$ tuyệt đối của kiến trúc RISC-V.
    \end{alertblock}
\end{frame}

% =============================================================================
% SLIDE 4: RISC_CORE INTEGRATION
% =============================================================================
\begin{frame}[fragile]{risc\_core Integration}
    \begin{columns}[T]
        \begin{column}{0.45\textwidth}
            \textbf{Nguyên tắc đóng gói:}
            \begin{itemize}
                \item Giữ nguyên $100\%$ giao tiếp Ports ra ngoài hệ thống Bus và mô hình đa nhân (8 cores) của nhóm.
                \item Bên trong ruột gọi (instantiate) 6 module con độc lập và thực hiện kết nối bằng dây dẫn (\texttt{wire}).
            \end{itemize}
        \end{column}
        
        \begin{column}{0.55\textwidth}
            \begin{block}{Cấu trúc đấu nối Hazard Controller trong Core}
\begin{lstlisting}
// --- PASTE DOAN CODE INSTANTIATE HAZARD_CONTROLLER TRONG CORE TAI DAY ---

\end{lstlisting}
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

% =============================================================================
% SLIDE 5: UNIT TESTING - PART 1
% =============================================================================
\begin{frame}{Unit Testing - Kiểm thử vách ngăn IF/ID và ID/EX}
    \begin{columns}[T]
        \begin{column}{0.5\textwidth}
            \begin{block}{1. Kết quả mô phỏng \texttt{pipe\_reg\_if\_id}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/if_id.png}
            \end{block}
            \textbf{Phân tích:} Minh chứng phản hồi chính xác khi tín hiệu \texttt{hold} dựng lên, giá trị PC và lệnh được khóa cứng qua xung clock.
        \end{column}
        
        \begin{column}{0.5\textwidth}
            \begin{block}{2. Kết quả mô phỏng \texttt{pipe\_reg\_id\_ex}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/id_ex.png}
            \end{block}
            \textbf{Phân tích:} Tín hiệu \texttt{clear} (Flush) kích hoạt thành công, xóa sạch các cờ điều khiển rác về bằng $0$ ngay chu kỳ sau.
        \end{column}
    \end{columns}
\end{frame}

% =============================================================================
% SLIDE 6: UNIT TESTING - PART 2
% =============================================================================
\begin{frame}{Unit Testing - Kiểm thử vách ngăn EX/MEM và MEM/WB}
    \begin{columns}[T]
        \begin{column}{0.5\textwidth}
            \begin{block}{3. Kết quả mô phỏng \texttt{pipe\_reg\_ex\_mem}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/ex_mem.png}
            \end{block}
            \textbf{Phân tích:} Dữ liệu địa chỉ và toán hạng lưu trữ từ ALU được đồng bộ đẩy sang bộ nhớ chính xác theo sườn lên của Clock.
        \end{column}
        
        \begin{column}{0.5\textwidth}
            \begin{block}{4. Kết quả mô phỏng \texttt{pipe\_reg\_mem\_wb}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/mem_wb.png}
            \end{block}
            \textbf{Phân tích:} Sửa lỗi sập hầm bằng chân \texttt{hold} thay thế \texttt{clear}. Khi gặp \texttt{mem\_wait}, tầng ghi được giữ nguyên, bảo toàn dữ liệu.
        \end{column}
    \end{columns}
\end{frame}