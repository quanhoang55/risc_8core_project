% =============================================================================
% SLIDE: HAZARD DETECTION & FORWARDING
% =============================================================================
\begin{frame}{Hazard Detection\& Forwarding}
    \begin{itemize}
        \item \textbf{Tư duy tích hợp:} Gom toàn bộ logic phát hiện Load-Use Stall, cứu dữ liệu (Forwarding) và quản lý bộ nhớ bận (\texttt{mem\_wait}) vào một bộ điều khiển tập trung để tối ưu tài nguyên.
    \end{itemize}

    \begin{block}{Sơ đồ khối điều khiển trung tâm hệ thống - Hazard Controller}
        \centering
        \begin{tikzpicture}[
            block/.style={draw, thick, fill=orange!10, draw=orange!80!black, minimum width=3.8cm, minimum height=2.4cm, rounded corners, font=\small\bfseries, align=center},
            wire_in/.style={->, >=stealth, thick, red!70!black},
            wire_out/.style={->, >=stealth, thick, green!50!black}
        ]
            % Hazard Block
            \node[block] (hz) at (0, 0) {Hazard Controller\\[0.1cm]\tiny (\texttt{hazard\_controller.v})};
            
            % Inputs bên trái (Các chân giám sát xung đột)
            \draw[wire_in] (-3.5, 0.6) -- node[above, font=\tiny\bfseries] {ex\_rs1 / ex\_rs2} (hz.170);
            \draw[wire_in] (-3.5, 0.1) -- node[above, font=\tiny\bfseries] {mem\_rd / wb\_rd} (hz.180);
            \draw[wire_in] (-3.5, -0.6) -- node[below, font=\tiny\bfseries] {mem\_wait / redirect} (hz.190);
            
            % Outputs bên phải (Tín hiệu điều khiển phản hồi)
            \draw[->, >=stealth, double, double distance=1.0pt, thick, green!50!black] (hz.15) -- node[above, font=\tiny\bfseries] {forward\_a / b [1:0]} (3.5, 0.45);
            \draw[wire_out] (hz.5) -- node[above, font=\tiny\bfseries, xshift=0.2cm] {if\_id\_hold / clear} (3.5, 0.15);
            \draw[wire_out] (hz.-5) -- node[below, font=\tiny\bfseries, xshift=0.2cm] {id\_ex\_hold / clear} (3.5, -0.15);
            \draw[wire_out] (hz.-15) -- node[below, font=\tiny\bfseries, xshift=0.1cm] {ex\_mem\_hold} (3.5, -0.45);
        \end{tikzpicture}
    \end{block}
\end{frame}

% =============================================================================
% SLIDE 4: RISC_CORE INTEGRATION
% =============================================================================
\begin{frame}[fragile]{RISC\_Core Integration}
    \begin{columns}[T]
        \begin{column}{0.45\textwidth}
            \textbf{Nguyên tắc đóng gói:}
            \begin{itemize}
                \item Giữ nguyên $100\%$ giao tiếp Ports ra ngoài hệ thống Bus và mô hình đa nhân (8 cores) của nhóm.
                \item Bên trong ruột gọi (instantiate) 6 module con độc lập và thực hiện kết nối bằng dây dẫn (\texttt{wire}).
            \end{itemize}
        \end{column}
        
        \begin{column}{0.55\textwidth}
            \begin{block}{Cấu trúc đấu nối Hazard Controller trong Core}
\begin{lstlisting}
// --- PASTE DOAN CODE INSTANTIATE HAZARD_CONTROLLER TRONG CORE TAI DAY ---

\end{lstlisting}
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

% =============================================================================
% SLIDE 5: UNIT TESTING - PART 1
% =============================================================================
\begin{frame}{Pipeline Register Unit Testing - IF/ID và ID/EX}
    \begin{columns}[T]
        \begin{column}{0.5\textwidth}
            \begin{block}{1. Kết quả mô phỏng \texttt{pipe\_reg\_if\_id}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/if_id.png}
            \end{block}
            \textbf{Phân tích:} Minh chứng phản hồi chính xác khi tín hiệu \texttt{hold} dựng lên, giá trị PC và lệnh được khóa cứng qua xung clock.
        \end{column}
        
        \begin{column}{0.5\textwidth}
            \begin{block}{2. Kết quả mô phỏng \texttt{pipe\_reg\_id\_ex}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/id_ex.png}
            \end{block}
            \textbf{Phân tích:} Tín hiệu \texttt{clear} (Flush) kích hoạt thành công, xóa sạch các cờ điều khiển rác về bằng $0$ ngay chu kỳ sau.
        \end{column}
    \end{columns}
\end{frame}

% =============================================================================
% SLIDE 6: UNIT TESTING - PART 2
% =============================================================================
\begin{frame}{Pipeline Register Unit Testing EX/MEM và MEM/WB}
    \begin{columns}[T]
        \begin{column}{0.5\textwidth}
            \begin{block}{3. Kết quả mô phỏng \texttt{pipe\_reg\_ex\_mem}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/ex_mem.png}
            \end{block}
            \textbf{Phân tích:} Dữ liệu địa chỉ và toán hạng lưu trữ từ ALU được đồng bộ đẩy sang bộ nhớ chính xác theo sườn lên của Clock.
        \end{column}
        
        \begin{column}{0.5\textwidth}
            \begin{block}{4. Kết quả mô phỏng \texttt{pipe\_reg\_mem\_wb}}
                \centering
                \includegraphics[width=0.9\textwidth]{design/test/mem_wb.png}
            \end{block}
            \textbf{Phân tích:} Sửa lỗi sập hầm bằng chân \texttt{hold} thay thế \texttt{clear}. Khi gặp \texttt{mem\_wait}, tầng ghi được giữ nguyên, bảo toàn dữ liệu.
        \end{column}
    \end{columns}
\end{frame}